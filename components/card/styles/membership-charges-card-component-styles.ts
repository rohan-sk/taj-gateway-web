import styled from "@emotion/styled"
import { Box, Typography } from "@mui/material"

export const MembershipChargesCardComponentBox = styled(Box)(() => ({
  display: "flex",
  flexDirection: "column",
  flexWrap: "wrap",
  alignItems: "center",
}))

export const MembershipDescriptionTypography = styled(Typography)(() => ({
  display: "flex",
  textAlign: "center",
}))
